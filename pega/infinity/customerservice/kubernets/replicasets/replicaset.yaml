apiVersion: apps/v1
kind: ReplicaSet
# metadata
metadata:
  name: myapp-replicaset
  labels:
    app: myapp
# spec
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 3
  template:
    metadata:
      name: nginx-10
      labels:
        app: myapp
        env: production
    spec:
      containers:
      - name: nginx
        image: nginx
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
